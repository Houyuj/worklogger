# Work Logger 用户指南

Work Logger 用于把每天零散的研究工作，逐步归档到周重点、月里程碑、年度目标和项目中。它适合实验设计、数据分析、文献阅读、组会准备、样品处理和论文写作等混合型研究工作。

## 先理解五个对象

| 对象 | 用途 | 例子 |
| --- | --- | --- |
| 项目 | 一段持续数月或数年的研究方向 | RNA ribozyme 工程化 |
| 年度目标 | 项目在某一年的主要成果 | 完成候选变体筛选与初步机制验证 |
| 月度里程碑 | 可在一个时间段内完成的阶段成果 | 完成 20 个变体的活性测定 |
| 周计划 | 有日期范围、归属月度里程碑的一周工作安排 | 7 月 20-26 日完成变体 1-5 的重复实验 |
| 任务 | 今天或某一天的可执行动作 | 配制反应缓冲液并完成变体 1 的测定 |

Protocol 在当前版本中显示为 **Experiments**。新建时只需输入标题，系统会自动在标题前添加当天日期；随后在同一份自由实验日志中输入正文、插入图片，并使用一级至四级标题组织内容。它也可关联多个任务。

## 第一次使用

1. 在 Windows 上双击 `setup.bat`，完成环境安装后双击 `run.bat`。
2. 打开浏览器中的 Dashboard 页面。
3. 在 Dashboard 的 **Research Plan** 区域，按当前需要创建项目、年度目标、月度里程碑或周计划；四个层级都可以单独创建。创建月度里程碑时选择月份会自动填入该月首日和末日；创建周计划时选择周会自动填入周一至周日。下方起止日期仍可直接修改，用于微调。
4. 创建后在同一区域选中当前正在推进的项目或目标。下一级会自动只显示与上一级有关的条目。
5. 需要完整链路时，再依次补充项目、年度目标、月度里程碑和周计划；不需要等全部建立完才能开始记录任务。

不用在第一天建立所有目标。先创建当前正在推进的一条链路，后续再逐步补充。

## 每天：快速记录任务

Dashboard 顶部的 Quick add 用于快速捕捉工作，例如：

- 分析 qPCR 原始数据
- 订购引物
- 阅读两篇结构生物学论文
- 修改组会汇报

输入任务并按 Enter 或 Add 即可。任务会立即出现在所选日期的任务列表中；若 Research Plan 已选择当前项目或目标，新任务会自动继承这些归属。未选择层级的任务仍是普通任务，之后可以在编辑任务时补充关联。

对于已经明确属于某个目标的工作，使用 **Create Detailed Task / Link to Experiment**：

1. 填写任务名称、日期、优先级和工作状态。
2. 如 Research Plan 已经选好当前工作位置，只需确认归属；否则在此选择需要的层级。
3. 如任务属于某个具体实验，选择对应的 Protocol。
4. 完成后勾选任务；完成状态会进入统计和报告。

建议：任务名称写成可验证的动作和产出，例如“完成 WT 与 V1 的三次重复并上传原始表格”，不要只写“做实验”。

## 每周：整理与复盘

每周固定留出 10-15 分钟完成以下步骤：

1. 在 Dashboard 的日期导航中回看本周任务，补全未分类的任务。
2. 标记已经完成的工作，并为未完成任务选择新的日期。
3. 检查周重点是否仍然支撑本月里程碑；不再重要的工作可以取消关联或延后。
4. 打开 Reports，查看 Weekly Report 的完成情况。
5. 导出 Excel 或 PDF，作为组会前的工作摘要。

周报反映的是任务完成情况，因此要确保任务日期正确，并在完成时勾选复选框。

## 每月：对齐里程碑

月初建立或调整本月里程碑；月末在 Reports 的 Monthly Report 中回顾任务和里程碑进展。若某个里程碑延期，先调整其日期范围，再决定是否拆分为更小的里程碑。

Gantt 页面用于查看项目计划的时间关系：

- 使用 Year / Month 切换年度和月度视角；左右箭头会切换对应的年份或月份。
- 使用减号和加号缩放时间轴。
- 在时间轴区域横向滚动查看全年；左右面板会保持垂直对齐。
- 年度视角显示项目、年度目标和月度里程碑；同一年度目标下日期重叠的月度里程碑会自动分行并排显示，不会互相覆盖。
- 月度视角的左列显示月度里程碑，色块显示其下的周计划；每个周计划色块范围取决于下属任务的最早和最晚日期。
- 点击月度条可修改状态或日期范围。
- 拖动图表底部的分隔线可改变可视高度。

建议每月查看一次 Gantt：它适合发现几个里程碑是否在同一时间段过度堆叠。

## Protocol：建立可复用实验记录

进入 Experiments 后创建新的 Protocol。一个可复用的 Protocol 可以包含：

- Overview：实验名称、状态和标签。
- Hypothesis：要验证的问题与预期结果。
- Methodology：试剂、步骤、仪器参数和方法表格。
- Results：原始结果摘要、计算表和图像。
- Conclusion：结论、局限与下一步。
- Progress Notes：当天观察和异常情况。

Methodology 和 Results 都支持添加多个命名表格；图片可上传到图片库。建议给表格命名，例如“反应体系配方”“变体活性汇总”“重复实验统计”，便于后续导出和复用。

当同一套实验方案需要重复使用时，使用 Duplicate 创建副本，再修改样品编号、日期和条件。原始 Protocol 作为模板保留，避免覆盖历史记录。

## 报告与备份

- Weekly Report：用于组会、周计划和本周完成情况。
- Monthly Report：用于月度里程碑复盘。
- Annual Report：用于年度总结、开题/中期检查或项目归档。
- Dashboard 的 Export Word：导出指定日期的任务和相关实验信息。

当前数据保存在项目目录的 `data/worklogger.db`。建议每周关闭应用后复制该文件到实验室云盘或受管理的备份位置。不要把唯一副本只留在电脑桌面或临时目录。

## 推荐工作节奏

| 时间 | 建议动作 |
| --- | --- |
| 每天开始前 | 记录 1-3 个最重要任务，必要时关联当前周重点 |
| 每天结束前 | 勾选完成项，给未完成项改期，补一条实验观察 |
| 每周 | 清理未分类任务，查看周报，调整下一周重点 |
| 每月 | 更新月度里程碑和 Gantt，导出月报 |
| 每年 | 回顾年度目标，导出年度报告，为下一年建立目标 |

## 常见问题

**任务没有出现在报告中**：检查任务日期是否落在报告区间内，以及任务是否已经保存。

**Gantt 图为空**：先创建项目、年度目标和带日期范围的月度里程碑；仅有独立任务不会形成甘特条。

**不知道任务归到哪个目标**：先快速添加，周复盘时再分类。记录工作本身比当下建立完美分类更重要。

**怎样安全升级或迁移电脑**：先备份 `data/worklogger.db`，在新电脑完成安装后再恢复该文件；恢复前保留原文件副本。

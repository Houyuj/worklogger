# Conda environment configuration for Work Logger
name: worklogger
channels:
  - defaults
  - conda-forge
dependencies:
  - python=3.10
  - pip
  - pip:
    - fastapi==0.104.1
    - uvicorn==0.24.0
    - sqlalchemy==2.0.23
    - pydantic==2.5.0
    - python-multipart==0.0.6
